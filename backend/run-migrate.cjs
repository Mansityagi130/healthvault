const { spawn } = require('child_process'); const p = spawn('npx.cmd', ['prisma', 'migrate', 'dev', '--name', 'org_uniques'], { stdio: ['pipe', 'pipe', 'pipe'] }); p.stdout.on('data', d => { const str = d.toString(); console.log(str); if (str.includes('We need to reset the database') || str.includes('fail')) p.stdin.write('y\n'); }); p.stderr.on('data', d => console.error(d.toString())); p.on('close', c => console.log('code', c));
